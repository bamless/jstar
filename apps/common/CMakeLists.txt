add_library(common STATIC colorio.h)
if(UNIX)
    target_sources(common PRIVATE colorio_posix.c)
else()
    target_sources(common PRIVATE colorio_none.c)
endif()
target_include_directories(common PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
